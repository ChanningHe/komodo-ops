services:
  rdma-exporter:
    container_name: rdma-exporter
    network_mode: host
    read_only: true
    volumes:
      - /sys/class/infiniband:/sys/class/infiniband:ro
    environment:
      - RDMA_EXPORTER_LOG_LEVEL=info
    image: ghcr.io/yuuki/rdma-exporter:0.4.1
networks: {}
