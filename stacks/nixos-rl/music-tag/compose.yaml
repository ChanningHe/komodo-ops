
services:
  music-tag:
    image: xhongc/music_tag_web:latest
    container_name: music-tag-web
    ports:
      - "38239:8002"
    volumes:
      - $PATH_MEDIACENTRE:/app/media:rw
      - $DOCKER_DATA/music-tag/config:/app/data
    restart: unless-stopped
    network_mode: bridge