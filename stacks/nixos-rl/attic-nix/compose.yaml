services:
  atticd:
    container_name: atticd
    image: ghcr.io/zhaofengli/attic:latest@sha256:18574aba70fc89d2b695273fbe2e7b2f8ad7e8e786b4cc535124fbe14bada1d0
    command: ["-f", "/attic/server.toml"]
    restart: unless-stopped
    # ports:
    #   - 8080:8080
    networks:
      - traefik_external_public_web
      - default
    volumes:
      - $DOCKER_DATA/attic-nix/atticd/server.toml:/attic/server.toml
      - $DOCKER_DATA/attic-nix/atticd/attic-data:/attic/storage
    depends_on:
      attic-postgres:
        condition: service_healthy
    healthcheck:
      test:
        [
          "CMD-SHELL",
          "wget --no-verbose --tries=1 --spider http://atticd:8080 || exit 1",
        ]
      interval: 15s
      timeout: 10s
      retries: 10
      start_period: 15s
    environment:
      - POSTGRES_DB=${ATTIC_POSTGRES_DB}
      - POSTGRES_USER=${ATTIC_POSTGRES_USER}
      - POSTGRES_PASSWORD=${ATTIC_POSTGRES_PASSWORD}
      - DATABASE_URL=${ATTIC_DATABASE_URL}
      #ATTIC_SERVER_TOKEN_HS256_SECRET_BASE64="<openssl rand 64 | base64 -w0>"
      - ATTIC_SERVER_TOKEN_HS256_SECRET_BASE64=${ATTIC_SERVER_TOKEN_HS256_SECRET_BASE64}
    labels:
      traefik.enable: true
      traefik.public: true
      traefik.http.routers.atticd.rule: Host(`attic.${DOMAIN_HSC}`)
      traefik.http.services.atticd.loadbalancer.server.port: 8080
      traefik.http.routers.atticd.service: atticd
      traefik.docker.network: traefik_external_public_web
      traefik.http.routers.atticd.entrypoints: https

  attic-postgres:
    container_name: attic-postgres
    image: postgres:17.6-alpine@sha256:ef257d85f76e48da1c64832459b59fcaba1a4dac97bf5d7450c77753542eee94
    restart: unless-stopped
    networks:
      - default
    volumes:
      - $DOCKER_DATA/attic-nix/postgres-data:/var/lib/postgresql/data
    environment:
      - POSTGRES_DB=${ATTIC_POSTGRES_DB}
      - POSTGRES_USER=${ATTIC_POSTGRES_USER}
      - POSTGRES_PASSWORD=${ATTIC_POSTGRES_PASSWORD}
      - DATABASE_URL=${ATTIC_DATABASE_URL}
      #ATTIC_SERVER_TOKEN_HS256_SECRET_BASE64="<openssl rand 64 | base64 -w0>"
      - ATTIC_SERVER_TOKEN_HS256_SECRET_BASE64=${ATTIC_SERVER_TOKEN_HS256_SECRET_BASE64}
    healthcheck:
      test:
        [
          "CMD-SHELL",
          "pg_isready -U ${ATTIC_POSTGRES_USER} -d ${ATTIC_POSTGRES_DB}",
        ]
      interval: 10s
      timeout: 5s
      retries: 5
networks:
  traefik_external_public_web:
    external: true
  default:
