SAFELINE_DIR=${DOCKER_DATA}/safeline-waf
IMAGE_TAG=latest
MGT_PORT=${SAFELINE_MGT_PORT}
POSTGRES_PASSWORD=${SAFELINE_POSTGRES_PASSWORD}
SUBNET_PREFIX=${SAFELINE_SUBNET_PREFIX}
IMAGE_PREFIX=chaitin
ARCH_SUFFIX=
RELEASE=
REGION=